import { MyEllipsisPipe } from './my-ellipsis.pipe';

describe('MyEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new MyEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
